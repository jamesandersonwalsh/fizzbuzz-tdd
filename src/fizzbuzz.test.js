import { expect } from 'chai'

import fizzbuzz from './fizzbuzz'

describe('#fizzbuzz', () => {
  it('Is a dummy placeholder test', () => {
    /*
    TODO: Start here.
      *  Rename the test to test your first case. I.E:
        it returns 'foobar'.
      * Test drive this. Write a case, get the tests green. Then refactor.
      * Do not write any more code until you have an additional test case written.
      * TDD your way to glory. Once you have a test for every case, your code should work the way
        you expect it to.
    */
    expect(fizzbuzz()).to.be.false
  })
})
